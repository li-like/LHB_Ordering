# 前后端文件交互详解

## 📁 项目文件结构对应关系

```
Uniapp_Odering/（前端）          ←→     DjangoProj/（后端）
├── utils/api.js                ←→     ├── wechat_auth/urls.py
├── utils/authExample.js        ←→     ├── wechat_auth/views.py  
├── utils/userManager.js        ←→     ├── wechat_auth/models.py
├── pages/login/login.vue       ←→     └── DjangoProj/urls.py
├── pages/index/index.vue
├── pages/family/family.vue
└── pages/profile/profile.vue
```

## 🔄 详细交互流程

### 1. 用户登录流程

#### 前端文件参与：
- `pages/login/login.vue` - 登录页面UI
- `utils/api.js` - API配置和请求封装
- `utils/authExample.js` - 登录逻辑示例
- `utils/userManager.js` - 用户信息管理

#### 后端文件参与：
- `DjangoProj/urls.py` - 主路由配置
- `wechat_auth/urls.py` - 认证相关路由
- `wechat_auth/views.py` - 登录视图处理
- `wechat_auth/models.py` - 用户数据模型

#### 详细交互步骤：

**第1步：前端发起登录请求**
```javascript
// 前端: pages/login/login.vue
import { AuthAPI } from '@/utils/api.js'

methods: {
  async handleLogin() {
    // 调用API配置中的登录接口
    const result = await AuthAPI.testLogin('test_user_001', '测试用户')
  }
}
```

**第2步：API配置文件处理请求**
```javascript
// 前端: utils/api.js
const AuthAPI = {
  async testLogin(testOpenid, nickname) {
    return request(API.auth.testLogin, 'POST', {
      test_openid: testOpenid,
      nickname
    })
  }
}
```

**第3步：请求发送到Django后端**
```
前端请求: POST http://127.0.0.1:8000/api/auth/test-login/
请求数据: {"test_openid": "test_user_001", "nickname": "测试用户"}
```

**第4步：Django路由分发**
```python
# 后端: DjangoProj/urls.py
urlpatterns = [
    path("api/auth/", include("wechat_auth.urls")),  # 转发到认证应用
]

# 后端: wechat_auth/urls.py  
urlpatterns = [
    path('test-login/', views.test_login_view, name='test_login'),  # 具体处理函数
]
```

**第5步：Django视图处理业务逻辑**
```python
# 后端: wechat_auth/views.py
@csrf_exempt
def test_login_view(request):
    if request.method == 'POST':
        data = json.loads(request.body)
        test_openid = data.get('test_openid', 'test_openid_123')
        nickname = data.get('nickname', '测试用户')
        
        # 使用模型创建或获取用户
        user, created = WechatUser.objects.get_or_create(...)
        
        return JsonResponse({
            'success': True,
            'data': {'user_id': user.id, ...}
        })
```

**第6步：数据库操作**
```python
# 后端: wechat_auth/models.py
class WechatUser(AbstractUser):
    openid = models.CharField(max_length=100, unique=True)
    nickname = models.CharField(max_length=100)
    # ... 其他字段
```

**第7步：响应返回前端**
```
后端响应: {"success": true, "data": {"user_id": 1, "openid": "test_user_001", ...}}
```

**第8步：前端处理响应**
```javascript
// 前端: utils/authExample.js
async function wechatLogin() {
  try {
    const backendRes = await AuthAPI.testLogin()
    // 保存用户信息到本地存储
    uni.setStorageSync('userInfo', backendRes.data)
    return backendRes.data
  } catch (error) {
    console.error('登录失败:', error)
  }
}
```

### 2. 获取用户信息流程

```
前端调用                     后端处理
pages/profile/profile.vue  →  wechat_auth/views.py (UserInfoView.get)
     ↓                           ↓
utils/api.js (AuthAPI)      →  wechat_auth/models.py (WechatUser)
     ↓                           ↓
HTTP GET请求               ←   JSON响应用户数据
```

### 3. 菜品数据流程（待开发）

```
前端页面                     后端接口（待创建）
pages/index/index.vue      →  restaurant/views.py
     ↓                           ↓
utils/api.js (餐厅API)      →  restaurant/models.py (Dish, Category)
     ↓                           ↓
显示菜品列表               ←   返回菜品数据
```

## 📋 具体文件职责

### 前端文件职责

| 文件 | 职责 | 对应后端 |
|------|------|----------|
| `utils/api.js` | API接口配置、HTTP请求封装 | 所有后端接口 |
| `utils/authExample.js` | 认证逻辑示例代码 | `wechat_auth/views.py` |
| `utils/userManager.js` | 用户信息本地管理 | `wechat_auth/models.py` |
| `pages/login/login.vue` | 登录页面UI和逻辑 | `wechat_auth/views.py` |
| `pages/index/index.vue` | 首页菜品展示 | `restaurant/views.py` (待开发) |
| `pages/family/family.vue` | 家庭管理页面 | `family/views.py` (待开发) |
| `pages/profile/profile.vue` | 个人中心页面 | `wechat_auth/views.py` |

### 后端文件职责

| 文件 | 职责 | 对应前端 |
|------|------|----------|
| `DjangoProj/urls.py` | 主路由配置 | `utils/api.js` 中的baseUrl |
| `wechat_auth/urls.py` | 认证相关路由 | `utils/api.js` 中的auth接口 |
| `wechat_auth/views.py` | 认证接口逻辑 | 登录相关的前端页面 |
| `wechat_auth/models.py` | 用户数据模型 | 用户信息相关的前端功能 |
| `restaurant/models.py` | 餐厅数据模型 | 菜品相关的前端页面 |
| `family/models.py` | 家庭数据模型 | 家庭功能相关的前端页面 |

## 🛠️ 如何修改前端以连接后端

### 第1步：修改 utils/userManager.js

```javascript
// 引入API配置
import { AuthAPI } from './api.js'

// 修改登录方法
async wechatLogin() {
  try {
    // 获取微信登录code
    const loginRes = await uni.login()
    const userProfileRes = await uni.getUserProfile({desc: '用于完善用户资料'})
    
    // 调用后端登录接口
    const backendRes = await AuthAPI.login(loginRes.code, userProfileRes.userInfo)
    
    // 保存到本地存储
    uni.setStorageSync('userInfo', backendRes.data)
    return backendRes.data
  } catch (error) {
    // 开发环境使用测试登录
    const testRes = await AuthAPI.testLogin()
    uni.setStorageSync('userInfo', testRes.data)
    return testRes.data
  }
}
```

### 第2步：修改 pages/login/login.vue

```vue
<script>
import { AuthAPI } from '@/utils/api.js'

export default {
  methods: {
    async wechatLogin() {
      uni.showLoading({title: '登录中...'})
      try {
        // 使用后端API登录
        const userInfo = await this.userManager.wechatLogin()
        uni.hideLoading()
        
        // 跳转到首页
        uni.switchTab({url: '/pages/index/index'})
        uni.showToast({title: '登录成功', icon: 'success'})
      } catch (error) {
        uni.hideLoading()
        uni.showToast({title: error.message, icon: 'none'})
      }
    }
  }
}
</script>
```

### 第3步：在其他页面获取数据

```javascript
// pages/index/index.vue - 获取菜品数据
import { API, request } from '@/utils/api.js'

export default {
  data() {
    return {
      dishes: []
    }
  },
  async onLoad() {
    try {
      // 调用后端获取菜品列表
      const result = await request(API.restaurant.dishes, 'GET')
      this.dishes = result.data
    } catch (error) {
      console.error('获取菜品失败:', error)
    }
  }
}
```

## 🔧 开发调试流程

1. **启动后端服务器**
   ```bash
   cd DjangoProj
   python manage.py runserver
   ```

2. **修改前端配置**
   - 确保 `utils/api.js` 中的 `baseUrl` 指向正确的后端地址

3. **在HBuilderX中调试**
   - 使用浏览器调试，可以直接访问本地Django服务器
   - 使用微信开发者工具需要配置域名白名单

4. **查看交互日志**
   - 前端：浏览器控制台或微信开发者工具控制台
   - 后端：Django控制台输出

这样就建立了完整的前后端交互体系！前端通过 `utils/api.js` 统一管理所有后端接口，后端通过 Django 的路由系统分发处理请求。
