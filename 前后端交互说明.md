# 情侣点餐 V2 前后端交互

## 运行时边界

- H5 与微信小程序共用 `Uniapp_Odering/pages/v2`、`components/v2`、`store` 和 `services/api`。
- Django 只暴露 `/api/v2/*`。所有空间、菜单、餐次和图片资源都会检查当前用户的 `Membership`。
- H5 使用邮箱密码注册/登录；小程序使用 `uni.login` 的临时 code，由后端换取 openid。
- 认证头为 `Authorization: Bearer <token>`，token 明文只在签发时返回，服务端保存摘要。

## 主要交互流程

```text
注册/登录
  → 获取情侣空间
  → 无空间时创建或用邀请码加入
  → 并行加载成员、外观、菜单、活动餐次
  → 发起今晚餐次
  → 双方添加想吃的菜
  → 确认共同菜单
  → 开始准备
  → 完成并生成不受后续改名影响的餐桌记忆
```

菜单和主题会在创建空间时初始化，因此新用户不需要先做配置。之后所有个性化都保存在空间或成员上，另一方重新进入后会看到同样的共享背景、主题和菜单，同时保留自己的头像与昵称。

## 个性化写入

1. 背景、头像、菜单封面和菜品图先上传至 `POST /api/v2/media-assets`。
2. 上传成功后将返回的绝对 `url` 写入对应资源。
3. 空间级外观写入 `PATCH /api/v2/spaces/{id}/appearance`；成员昵称、头像、头像框写入 `PATCH /api/v2/spaces/{id}/members/me`。
4. 菜单名称、封面和图片样式写入 `PATCH /api/v2/menus/{id}`；菜品使用 `POST/PATCH /api/v2/sections/{id}/dishes` 和 `/api/v2/dishes/{id}`。

外观 API 只接受设计 token、十六进制颜色与受控样式枚举，不允许注入任意 CSS。这使用户可以大幅个性化，又不会破坏小程序与 H5 的可用性。

## 错误处理

`services/api/client.js` 统一处理 HTTP 错误。401 会清理本地 token 并返回登录页；字段校验错误会提取首条有意义的中文信息展示，而不是只显示 400。

完整路由、字段和状态约束见 `ordering_backend/README_V2.md`。
