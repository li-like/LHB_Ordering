# 情侣点餐 V2 启动指南

项目由 Django REST 后端和同一套 UniApp 前端组成，前端同时输出 H5 网页和微信小程序。V2 的主入口是 `pages/v2/*` 和 `/api/v2/*`；仓库中的旧页面与旧 Django app 仅作参考，不再注册到运行时。

## 1. 启动后端

```powershell
cd ordering_backend
python -m pip install -r requirements.txt
python manage.py migrate
python manage.py runserver 127.0.0.1:8000
```

管理后台位于 `http://127.0.0.1:8000/admin/`，需要时可使用 `python manage.py createsuperuser` 创建管理员。

开发环境可使用默认值；生产环境必须按 `ordering_backend/.env.example` 设置系统环境变量，尤其是随机 `DJANGO_SECRET_KEY`、`DJANGO_DEBUG=False`、域名、CORS 和微信 AppSecret。

## 2. 启动 H5

```powershell
cd Uniapp_Odering
npm install
npm run dev:h5
```

浏览器默认访问 Vite 在终端中打印的本地地址。前端开发环境默认请求 `http://127.0.0.1:8000/api/v2`。

如后端不在本机，先复制 `Uniapp_Odering/.env.example` 为 `.env.local`，并修改：

```dotenv
VITE_API_BASE_URL=https://your-api.example.com/api/v2
```

## 3. 运行微信小程序

```powershell
cd Uniapp_Odering
npm install
npm run dev:mp-weixin
```

使用微信开发者工具导入 `Uniapp_Odering/dist/dev/mp-weixin`。真机无法访问电脑自身的 `127.0.0.1`，请将 `VITE_API_BASE_URL` 改为已配置小程序合法域名的 HTTPS API。微信 AppID 在 `manifest.json` 配置，AppSecret 只能放在后端环境变量中。

## 4. 验证

```powershell
cd ordering_backend
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test v2 -v 2

cd ..\Uniapp_Odering
npm run build:h5
npm run build:mp-weixin
```

首次注册后可创建情侣空间。系统会自动生成四套主题、三个栏目和八道示例菜，然后可在“个性化”中改背景、主题、卡片、导航文案、双方头像和菜单展示。

详细产品与数据设计见 `docs/V2_PRODUCT_AND_ARCHITECTURE.md`，后端 API 见 `ordering_backend/README_V2.md`。
