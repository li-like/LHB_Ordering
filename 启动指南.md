# 柯梦快点餐 - 项目启动指南

## 项目概述

这是一个完整的微信小程序点餐系统，包含：
- **前端**: UniApp开发的微信小程序 (位于 `Uniapp_Odering/`)
- **后端**: Django REST API服务器 (位于 `DjangoProj/`)

## 快速启动

### 1. 启动Django后端服务器

```bash
# 进入Django项目目录
cd DjangoProj

# 检查配置
python manage.py check

# 创建测试数据 (可选)
python manage.py create_test_data

# 启动开发服务器
python manage.py runserver 127.0.0.1:8000
```

服务器启动后，您可以访问：
- API根路径: http://127.0.0.1:8000/
- Django管理后台: http://127.0.0.1:8000/admin/ (需要先创建超级用户)

### 2. 测试API接口

```bash
# 运行API测试脚本
python test_api.py
```

### 3. 配置微信小程序前端

1. 打开 `Uniapp_Odering/` 目录
2. 查看 `utils/api.js` 配置文件 - **负责连接Django后端**
3. 参考 `utils/authExample.js` 集成后端API
4. 使用 `utils/userManagerWithBackend.js` 替换原有用户管理器
5. 参考 `pages/loginWithBackend.vue` 修改登录页面
6. 在HBuilderX中运行项目

### 4. 验证前后端集成

```bash
# 运行集成检查脚本
python integration_check.py

# 查看集成指南
python integration_check.py guide

# 启动服务器
python integration_check.py start
```

## 📁 文件交互关系图

```
前端 (UniApp)                    后端 (Django)
├── utils/api.js               ←→ DjangoProj/urls.py (路由配置)
├── utils/userManagerWithBackend.js ←→ wechat_auth/views.py (登录逻辑)
├── pages/loginWithBackend.vue ←→ wechat_auth/models.py (用户模型)
└── pages/index/index.vue      ←→ restaurant/views.py (菜品接口)

数据流向：
前端发起请求 → utils/api.js → Django路由 → 视图处理 → 数据库操作 → JSON响应 → 前端处理
```

## 🔄 具体交互流程

### 用户登录流程示例：

1. **前端**: `pages/loginWithBackend.vue` 用户点击登录
2. **前端**: `utils/userManagerWithBackend.js` 调用 `wechatLogin()`
3. **前端**: `utils/api.js` 发送 POST 请求到 `/api/auth/login/`
4. **后端**: `DjangoProj/urls.py` 路由转发到 `wechat_auth/urls.py`
5. **后端**: `wechat_auth/views.py` 处理登录逻辑
6. **后端**: `wechat_auth/models.py` 创建或更新用户数据
7. **后端**: 返回 JSON 响应 `{"success": true, "data": {...}}`
8. **前端**: `userManagerWithBackend.js` 保存用户信息到本地
9. **前端**: 跳转到首页

### API调用示例代码：

```javascript
// 前端调用后端API的标准流程
import { AuthAPI } from '@/utils/api.js'

// 1. 登录
const userInfo = await AuthAPI.login(code, userInfo)

// 2. 获取用户信息  
const result = await AuthAPI.getUserInfo()

// 3. 更新用户信息
await AuthAPI.updateUserInfo({nickname: '新昵称'})
```

```
UniappProj/
├── DjangoProj/                 # Django后端项目
│   ├── DjangoProj/            # 项目配置
│   │   ├── settings.py        # Django设置
│   │   └── urls.py           # URL路由
│   ├── wechat_auth/          # 微信认证应用
│   │   ├── models.py         # 用户模型
│   │   ├── views.py          # API视图
│   │   └── urls.py           # 认证相关路由
│   ├── restaurant/           # 餐厅管理应用
│   │   ├── models.py         # 菜品、订单模型
│   │   └── management/       # 管理命令
│   ├── family/               # 家庭管理应用
│   │   └── models.py         # 家庭、成员模型
│   ├── static/               # 静态文件
│   ├── media/                # 媒体文件
│   ├── test_api.py           # API测试脚本
│   └── README.md             # 后端文档
└── Uniapp_Odering/           # UniApp前端项目
    ├── pages/                # 页面
    │   ├── login/            # 登录页面
    │   ├── index/            # 首页
    │   ├── family/           # 家庭页面
    │   └── profile/          # 个人中心
    ├── utils/                # 工具类
    │   ├── api.js            # API配置
    │   ├── authExample.js    # 认证示例
    │   └── userManager.js    # 用户管理
    └── static/               # 静态资源
```

## 当前已实现的功能

### 后端 (Django)

✅ **用户认证模块**
- 微信用户模型 (WechatUser)
- 登录日志记录 (WechatLoginLog)
- 微信登录API接口 `/api/auth/login/`
- 测试登录接口 `/api/auth/test-login/`
- 用户信息接口 `/api/auth/user-info/`

✅ **数据模型**
- 菜品分类 (Category)
- 菜品信息 (Dish)  
- 购物车 (Cart)
- 订单管理 (Order, OrderItem)
- 家庭管理 (Family, FamilyMember)
- 家庭订单 (FamilyOrder)

✅ **基础设施**
- 数据库迁移完成
- Django管理后台配置
- 测试数据生成命令
- API接口文档

### 前端 (UniApp)

✅ **页面结构**
- 登录页面 (pages/login/login.vue)
- 首页 (pages/index/index.vue)
- 家庭页面 (pages/family/family.vue)
- 个人中心 (pages/profile/profile.vue)

✅ **工具类**
- 用户管理器 (utils/userManager.js)
- API配置文件 (utils/api.js)
- 认证示例代码 (utils/authExample.js)

## 🎯 家庭点餐功能重构方案

### 核心理念
**家庭内部点餐** = 家庭成员表达用餐需求 + 管理员统筹决策 + 记录用餐历史

### � 功能模块重构

#### 1. 首页重构 (index页面)
**问题**: 当前不像点餐小程序，更像菜谱展示
**方案**: 
- 保留: 随便来点、菜谱大全、饮食记录
- 移除: 今日推荐菜品
- 重构: 餐品分类管理 (用户自定义分类 + 图标)
- 新增: 点餐功能 (添加到想吃清单)

#### 2. 餐品分类 & 餐品管理
- 用户可自定义分类 (早餐/午餐/晚餐/汤品等)
- 每个分类支持自定义图标/图片
- 餐品可关联菜谱或独立管理
- 支持从菜谱库快速导入餐品

#### 3. 简化点餐流程
```
选择餐品 → 添加到想吃清单 → 提交到家庭汇总 → 管理员确认 → 生成用餐记录
```

#### 4. 家庭页面重构
**移除**: 用餐时间设置、复杂统计
**重点**: 今日点餐汇总 + 管理员确认制作清单
**显示**: 成员点餐需求 + 管理员决策界面

#### 5. 用餐记录页面增强
**整合**: 历史记录 + 饮食偏好 + 统计分析

## 🚀 实现步骤 (4个阶段)

### Phase 1: 数据模型设计 (1-2天)
🔲 **后端数据模型**
- 餐品分类模型 (MealCategory)
- 餐品模型 (MealItem) 
- 点餐需求模型 (MealRequest)
- 制作确认模型 (MealConfirmation)

🔲 **数据库迁移**
- 创建新表结构
- 数据关联设计
- 测试数据生成

### Phase 2: 后端API开发 (2-3天)
🔲 **餐品管理API**
- 分类CRUD接口
- 餐品CRUD接口
- 菜谱关联接口

🔲 **点餐功能API**
- 提交点餐需求接口
- 获取家庭点餐汇总接口
- 管理员确认制作接口
- 生成用餐记录接口

### Phase 3: 前端页面重构 (3-4天)
🔲 **首页重构**
- 移除今日推荐模块
- 重构餐品分类展示
- 添加分类管理功能
- 实现点餐选择界面

🔲 **家庭页面重构**
- 简化页面结构
- 添加点餐汇总模块
- 实现管理员确认界面
- 移除冗余功能

🔲 **用餐记录页面增强**
- 整合饮食偏好设置
- 优化历史记录展示
- 添加统计分析功能

### Phase 4: 功能整合 & 测试 (1-2天)
🔲 **前后端联调**
- API接口测试
- 数据流测试
- 用户体验优化

🔲 **功能验证**
- 完整流程测试
- 边界情况处理
- 性能优化

## 待开发功能 (更新)

### 当前待开发重点

🔲 **核心点餐功能**
- 餐品分类管理
- 点餐需求收集
- 家庭点餐汇总
- 管理员确认制作
- 自动生成用餐记录

🔲 **页面重构**
- 首页餐品分类重构
- 家庭页面简化
- 用餐记录页面增强

🔲 **用户体验优化**
- 点餐流程简化
- 界面响应优化
- 数据同步机制

## 开发环境要求

### 后端
- Python 3.8+
- Django 5.2+
- SQLite (开发环境)

### 前端  
- HBuilderX
- UniApp框架
- 微信开发者工具

## 配置说明

### Django配置
- 数据库: SQLite (开发环境)
- 调试模式: 已启用
- CSRF保护: 已禁用 (便于前端调试)
- 允许的主机: 所有 (仅开发环境)

### 微信小程序配置
- AppID: 需要配置真实的微信小程序AppID
- AppSecret: 需要配置对应的AppSecret
- 服务器域名: 开发环境使用本地地址

## 注意事项

1. **开发环境**: 当前配置仅适用于本地开发测试
2. **微信API**: 暂未集成真实微信API，使用模拟登录
3. **数据安全**: 生产环境需要启用CSRF保护和HTTPS
4. **依赖包**: 如需图片上传功能，需安装Pillow包
5. **域名配置**: 生产环境需要配置合法域名和SSL证书

## 联系方式

如有问题，请查看代码注释或联系开发团队。
